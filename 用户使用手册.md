# 56 WiFi 时钟 用户使用手册

> 本手册面向最终用户：涵盖**如何刷固件**、**日常使用方法**与**常见故障排查**。硬件平台（STC15W408AS + TM1639 数码管 + DS1302 + ESP-01S 的「56」WiFi 时钟）来自 [56dz 数码之家](https://www.56dz.com/p/3759.html)，本文所述按键、显示与配网逻辑均与实机一致。

## 目录

- [一、产品概览](#一产品概览)
- [二、刷固件指南](#二刷固件指南)
- [三、日常使用](#三日常使用)
- [四、Web 配置](#四web-配置)
- [五、排故手册](#五排故手册)
- [六、重要提示](#六重要提示)

---

## 一、产品概览

- **主屏（大数码管）**：显示时间 / 日期 / 温度，按 UP 键切换。
- **下排小管（SMG1）**：时间模式下固定显示「温度」或「日期(日)」二选一（Web 配置）。
- **上排小管（SMG2）**：时间模式下显示秒，日期模式下显示年份。
- **WiFi**：内置 ESP8266 模块，支持配网后自动对时（NTP）。
- **蜂鸣器**：闹钟、倒计时结束、按键确认提示。
- **按键**：UP 键与 SET 键两个。

| 指示灯 | 位置 | 含义 |
| --- | --- | --- |
| 红色 LED | 51 侧 | 为「联网成功」指示（Web 可开关，`led_en`） |
| 蓝色 LED6 + 模块板载灯 | ESP 侧 | **仅 AP 配网期间常亮**（等待配网）；STA 工作与待机时熄灭 |

---

## 二、刷固件指南

> 固件分两部分，需分别烧录：
> - **51 固件（主控，STC15W408AS）**：编译产物 `firmware/STC/out/firmware.hex`
> - **8266 固件（WiFi 模块，ESP-01S）**：编译产物 `firmware/8266/.pio/build/esp01_1m/firmware.bin`

### 2.1 准备

- 电脑需安装 **Python 3**（烧写 8266 用，可用 `python --version` 确认）。
- 51 固件烧录需要 Windows 工具 **STC-ISP**（STC 官方软件）。
- 一根 USB 转 TTL 串口线（或配套的自动下载器）。
- 如从源码编译：需要 SDCC 编译器（51）与 PlatformIO（8266），详见 `firmware/STC/BUILD.md` 与 `firmware/8266/编程计划.md`。

### 2.2 烧录 51 固件（STC15W408AS）

STC 芯片采用"**先点下载、再上电**"的冷启动方式：

1. 用 USB-TTL 连接：**TX → MCU RX(P3.0)、RX → MCU TX(P3.1)、GND 共地**。MCU 接好供电，但**先不要上电**。
2. 打开 **STC-ISP** 软件，芯片型号选 `STC15W408AS`，IRC 频率选 **`11.051 MHz`**（本板实测值，务必选对）。
3. 选择要烧录的固件：打开 `firmware/STC/out/firmware.hex`。
4. 点击「**下载/编程**」，然后立刻给 MCU **上电**（或按复位键），等待烧录进度走完即可。

命令行用户也可用 `stcgal`（需自行安装）：

```bash
python -m stcgal -P stc15 -p COM端口 out\firmware.hex
```

> 需要先在 `firmware/STC` 目录运行 `build.bat` 才会生成 `out\firmware.hex`。

### 2.3 烧录 8266 固件（ESP-01S）

项目已提供一键脚本 `firmware/8266/flash.bat`（自动定位 esptool.py，支持 `PLATFORMIO_CORE_DIR` 环境变量覆盖）：

```bat
cd firmware\8266
build.bat            :: 编译 8266 固件（需要已装 PlatformIO：python -m platformio run）
flash.bat            :: 烧录到默认串口 COM3
flash.bat COM5       :: 指定串口烧录
```

手动命令（不依赖脚本时）：

```bash
python "C:\Users\你的用户名\.platformio\packages\tool-esptoolpy\esptool.py" ^
      --port COM3 --baud 921600 --before default_reset ^
      write_flash 0x0 ".pio\build\esp01_1m\firmware.bin"
```

- 烧录成功标志：末行出现 `Hash of data verified.`
- **不要使用 `pio run -t upload`**：本项目接的是自动下载器（DTR/RTS 硬复位时序），esptool 默认复位方式与其不匹配，会烧录失败/挂起。

---

## 三、日常使用

### 3.1 按键速查表

| 操作 | 功能 |
| --- | --- |
| 单击 **UP** | 切换大屏显示：时间 → 日期 → 温度 → 时间…循环 |
| 双击 **UP** | 恢复走时（回到时间显示） |
| 长按 **UP** | 进入 / 退出**计时器** |
| 单击 **SET** | 进入**亮度调节**（数码管闪显档位，UP 加减，再单击 SET 确认退出） |
| 双击 **SET** | **手动对时** + 大屏显示 `P`+IP 末段约 3 秒（同时唤醒 WiFi 联网校时） |
| 长按 **SET** | 进入**时间设置**（见 3.3） |
| **两键同按 ≥ 5 秒** | **重新配网**：清除已保存的 WiFi 凭据、恢复默认 AP 密码，重新进入配网模式 |

**闹钟 / 倒计时响铃时：**

| 操作 | 功能 |
| --- | --- |
| 按 **UP** | **贪睡**（过一会儿再响一次，间隔由 Web 设定） |
| 按 **SET** | **取消全部响铃**（蜂鸣两声确认，之后不再响） |

**关屏时段内**（Web 设置的熄屏窗口）：按任意键可**临时点亮数码管 10 秒**。

### 3.2 显示内容对照

| 大屏 | 下排 SMG1 | 上排 SMG2 | 说明 |
| --- | --- | --- | --- |
| 时间 `HH:MM` | 温度 °C 或 日期(日)（Web 选） | 秒 `SS` | 默认状态，冒号每秒闪烁 |
| 日期 `MMDD`（月/日） | 星期 0–6（0=周日） | 年份 `YY` | 单击 UP 切换到 |
| 温度（符号+数字+C/F） | 熄灭 | 熄灭 | 单击 UP 切换到；温度单位由 Web 设 |

- **星期显示约定**：0 = 周日，1 = 周一 … 6 = 周六（数码管直接显示 0–6 数字）。
- **温度显示**：传感器故障时兜底显示 25°C。

### 3.3 时间设置（长按 SET）

1. **长按 SET** 进入设置：当前字段闪烁。
2. 字段顺序：**年 → 月 → 日 → 星期 → 时 → 分 → 秒**。
3. 按 **UP** 当前字段 +1（月到 12 自动回 1、闰年自动感知、星期 0–6）。
4. 单击 **SET** 跳到下一个字段；到「秒」后单击 SET **保存退出**（蜂鸣一声确认）。

### 3.4 计时器（长按 UP）

1. 长按 **UP** 进入计时器（大屏显示 `MM:SS`）。
2. 单击 **UP**：开始 / 停止（暂停后再次单击继续累计）。
3. 单击 **SET**：复位归零 `00:00`。
4. 长按 **UP** 退出计时器、恢复走时。计时上限 **99:59**。

---

## 四、Web 配置

设备有**两个独立网页**，职责不同：

### 4.1 AP 配网页（首次使用 / 换 WiFi 时）

仅用于填写**WiFi 账号密码**，不含其他设置。

1. 手机或电脑搜索 WiFi 热点 **`56dz network clock`**，输入默认密码 `12345678` 连接（首次可连，详见 §5.4）。
2. 浏览器打开 **`http://192.168.4.1`**。
3. 页面会自动列出附近的 WiFi（带信号强度，点击即自动填入名称），选好后输入 WiFi **密码**，点"连接"。
4. 配网成功后热点自动关闭，时钟自动联网对时。

> 提示：公交场景（手机做热点）时，手机自己连不上 `56dz` 热点，首次配网请用电脑连接热点填写手机热点账号；配网成功后再把手机切换回自身热点即可。

### 4.2 STA 配置页（日常全功能设置）

WiFi 配好后，时钟已连入局域网，可访问完整设置页：

1. **双击 SET**：时钟显示 `P`+IP 末段约 3 秒（唤醒 WiFi 并显示地址后段）。
2. 电脑 / 手机连上**同一个路由器**，浏览器输入时钟的完整 IP（即 `P` 后数字对应的完整网段地址；路由器的设备列表里也能查到）打开配置页。

可设置项目（单页表单，改完点保存即实时生效到数码管）：

- 大屏自动轮播开关（固定时间 / 每分钟自动轮换 时间→日期→温度）
- 亮度（自动 / 手动 1–8 档）
- 温度单位 °C / °F、**温度参考校正**（输入一个靠谱温度计测出的真值，时钟自动回算补偿）
- 下排小管显示内容（温度 / 日期）
- **3 组闹钟**（开关 + 时 + 分）
- 时区（默认 +8）与夏令时
- 关屏时段（起止时间，可禁用）
- 贪睡时长（5 / 10 分钟）
- 状态灯开关（红色 LED，联网成功后点亮）
- 倒计时（预设时长 + 开始 / 暂停 / 取消）
- AP 密码修改、WiFi 账号修改

### 4.3 关于"待机断网"（重要）

时钟平时处于**伪待机省电模式**（WiFi 收发器关闭）。因此：

- **STA 配置页平时打不开是正常设计，不是故障**。
- 每次 **双击 SET** 会唤醒 WiFi 并保持约 5 分钟，此窗口内可访问配置页；期间每次网页操作都会续期，**闲置 5 分钟自动断回待机**。
- 需要再次配置时，再双击 SET 唤醒即可。
- 对时会自动进行：上电、每天 00:00 / 12:00、或双击 SET 时。

---

## 五、排故手册

### 5.1 上电蜂鸣器响 3 声（未检测到 WiFi 模块）

- 现象：每次上电，蜂鸣器「哔哔哔」三声。
- 原因：51 主控等待 ESP8266 模块的启动握手（约 5 秒窗口）超时，判定模块未连接，进入调试模式。
- 处理：
  1. 检查 51 与 ESP-01S 的连接：ESP-RX → MCU-TX(P3.1)、ESP-TX → MCU-RX(P3.0)，不要接反。
  2. 检查 ESP-01S 是否已烧入本项目的 8266 固件（见 §2.3）；空模块/原厂固件不会发握手帧。
  3. 确认供电：ESP-01S 需 3.3V 供电，电流不足会导致其不启动。
  4. 重新上电：模块正常后三声不再出现，时钟进入正常模式。

### 5.2 数码管全不亮 / 暗

1. 检查供电：主板 5V、单片机供电是否正常。
2. 亮度是否被调暗或被**关屏时段**熄灭（熄屏时按任意键若可点亮 10 秒即正常）。
3. 检查 MCU（STC15）与 TM1639 的三线连接：STB=P2.3、CLK=P2.4、DIO=P2.5。

### 5.3 时间不对 / 断电后时间清零

- 现象：时间误差大，或断电后不走时。
- 处理：
  1. 正常走时依赖 **DS1302 与 CR1220 备用电池**：确认备用电池在位、**正极朝上**。
  2. 电池没电时断电时间即清零，装好新电池后重新设置时间（长按 SET，见 §3.3）。
  3. 时间长期不准可双击 SET 联网对时；确认其所在 WiFi 能访问互联网。

### 5.4 连不上 AP 热点「56dz network clock」

1. 确认时钟已上电、处在**未配网 / 配网失败**状态（热点才存在）。
2. 若已配过网但不可见：**两键同按 ≥ 5 秒** 清除凭据并切换回配网模式，热点重新出现。
3. 若热点能搜到但**连不上**：输入 AP 密码时确认使用默认 `12345678`（或被改过的密码）；忘记密码时用两键 ≥5s 恢复默认。
4. 连接后浏览器打开 `192.168.4.1`（不是其他地址）。

### 5.5 温度显示固定 25°C / 温度明显不准

- 固定显示 25°C：温度传感器（NTC，P1.1）开路 / 短路，固件兜底显示 25°C。检查传感器连接。
- 温度整体偏移：用 Web「温度参考校正」输入一个可靠参考温度一键矫正（见 §4.2）。

### 5.6 闹钟不响 / 到点没反应

1. 先在 **STA 配置页**确认：闹钟开关已打开、时 / 分设置正确。
2. 确认时钟时间正确（联网对时或手设后，见 §3.3）。
3. 响铃时长约 1 分钟，期间按 UP 贪睡、SET 取消；若没听到，检查蜂鸣器是否被关闭或电路。

### 5.7 烧录不成功

- **51 烧录**：确认「芯片型号 STC15W408AS」「IRC 频率 11.051MHz」「串口选择正确」；严格遵守"先点下载、再上电"顺序。换一根串口线或降低波特率再试（STC-ISP 常见问题）。
- **8266 烧录**：确认串口号正确、是空闲的自动下载器；不要用 `pio run -t upload`（见 §2.3）；若失败，确认固件已编译（`build.bat` 成功）且串口未被串口助手占用。

---

## 六、重要提示

- **AT 固件 / 测试固件请勿烧入本机**：只有烧入本项目附带的 8266 固件，51 才能识别模块并正常通信。
- **8266 模块接口电平**：51 的 TXD/RXD（P3.1/P3.0）必须保持准双向模式，不能强推挽，否则会烧毁 ESP8266 的输入脚。日常使用无需关注，升级固件或接线时请勿改动这两脚的驱动配置。
- 本项目为开源项目，完整文档见 `README.md`、`plan/` 目录（原理图、协议、功能规格）；源码级排故请参考 `firmware/STC/BUILD.md` 与 `firmware/8266/编程计划.md`。