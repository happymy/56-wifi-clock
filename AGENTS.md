# 56-wifi-clock 项目规则（AI 必须遵守）

适用于 STC15W408AS + ESP-01S 的 WiFi 时钟固件（51 端：`firmware/STC`，驱动复用 `demo/_common`）。

## 1. 硬件平台与定时器
- MCU 为 **STC15W408AS（STC15W 系列）**。该芯片**没有独立的 Timer 1（定时器1）模块**，可用定时器只有 **T0 与 T2**（另可查手册确认是否有 PCA 可用）。
- **严禁在代码中使用 Timer 1 / 中断号 3 产生时序或节拍**。需要系统节拍时改用 T0、T2 或 PCA，且必须先查官方手册确认存在与配置方式。
- 注意：demo 中 T0 已被 TM1639 软件 PWM（极暗调光）占用、T2 被 UART1 波特率源占用。复用 `demo/_common` 驱动时须据此规划，不要抢占这两者的定时器用途。

## 2. IO 模式安全红线（会烧硬件）
- **强推挽模式会直接烧毁外设**：若把 STC15 的 TXD 脚（P3.1）从“准双向口”误设为“强推挽输出”，5V 信号强电流直灌会瞬间烧毁 ESP8266 的 RX 脚；RXD（P3.0）同理。
- **P3.0 / P3.1（UART）必须保持准双向口**（P3M1/P3M0 对应位 = 00）。禁止为这两脚配置强推挽。
- 其它需推挽的脚（如 BEEP P2.1）须确认不连接到 ESP 或其它 3.3V 器件后再设。

## 3. 代码来源与硬件冻结
- 严格依照 `demo/` 目录下已验证的代码：`clock-bringup`、`ds1302-clock`、`hw-test` 及 `demo/_common`（`tm1639.c`/`ds1302.c`/`stc15.h` 等）。这些是经实验验证的权威参考——复用其驱动函数与初始化顺序，**不要另起一套或猜测 API**。
- 硬件设计已通过 demo 测试代码验证并**冻结，原理图不可更改**。引脚/电平以 demo 为准。

## 4. 资料查询：严禁猜测
- 任何寄存器、位定义、地址、外设模式、时序（IAP、UART、ADC、定时器、PCA 等）一律**查询 STC 官方手册并严格遵守**。
- **严禁猜测**任何寄存器位、地址或外设是否存在。不确定时先查官方手册或 `demo/` 中已验证的用法，再动手。

## 5. 来自 demo 验证的关键事实（直接复用，勿改）
- **EEPROM（Data Flash）IAP 地址范围 = 0x0000–0x13FF**（扇区 512B）；IAP 等待状态 `IAP_CONTR = 0x84`（≤12MHz）。**禁止越界地址**（如 0x2000）。
- **BEEP（P2.1）经 S9012 PNP，active-low**：拉低=响，拉高=静音（demo：`P2 &= ~0x02` 响、`P2 |= 0x02` 静）。
- 引脚：TM1639 `STB=P2.3 / CLK=P2.4 / DIO=P2.5`；DS1302 `CE=P1.3 / IO=P1.4 / SCLK=P1.5`；按键 `UP=P3.2`、`SET=P3.3`（active-low）；`LED=P1.2`；`LDR=P1.0`；`NTC=P1.1`。
- UART1 波特源用 Timer2（`AUXR` 置 `S1BRS=1`，`T2L=0xE0,T2H=0xFE` @ 9600 8N1），demo 已验证。
- 10 位 ADC：`ADC_RES<<2 | ADC_RESL&0x03`；`ch 0=P1.0 光敏, 1=P1.1 热敏`。

## 6. MCU 与 ESP8266 共用一个串口（关键约束）
- **STC15 与 ESP-01S 仅通过 UART1（P3.0/P3.1）这一条串口通信**，没有第二条串口给 MCU 做调试输出。
- 因此：**禁止为调试目的另接/另配串口占用 P3.0/P3.1**；任何 UART 配置（波特率、数据位、校验）改动都会同时影响与 ESP 的链路，必须保持与 ESP 约定一致（默认 9600 8N1，由 Timer2 产生波特源，demo 已验证）。
- 51 端的运行日志（`dbg_log`）写入 EEPROM 环形区，而非串口；要查运行日志需读取 EEPROM 而非看串口输出。
- 新增串口调试功能：由配置项 `g_cfg.debug_uart` 开关（默认 1，仅 bring-up 用）。开启时经 UART1 打印启动横幅/按键/网络/时间等调试信息；关闭时 `uart_puthex` 直接返回、不发任何字节，**不影响 8266 链路**。受芯片 256B IRAM 限制，调试输出仅提供 1 字节 hex（`uart_puthex`），字符串由调用方逐字节 `uart_putchar` 拼；**烧入 ESP 固件前必须把 `debug_uart` 置 0**（CFG_VERSION bump 触发默认重载，或串口下发配置）。
